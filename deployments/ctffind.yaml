apiVersion: apps/v1
kind: Deployment
metadata:
  name: ctffind
  namespace: em
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ctffind
  template:
    metadata:
      labels:
        app: ctffind
    spec:
      securityContext:
        runAsUser: 37679
        runAsGroup: 37829
      containers:
      - name: ctffind-runner
        image: gcr.io/diamond-pubreg/em/ctffind:relion
        imagePullPolicy: Always
        resources:
          requests:
            cpu: "250m"
        command: ["/bin/sh"]
        args:
          - -c
          - >-
              source "/conda/etc/profile.d/conda.sh" && conda activate /install/pythonenv && zocalo.service -s CTFFind -e devrmq
        env:
        - name: KUBERNETES
          value: "1"
        - name: ZOCALO_CONFIG
          value: "/zocalo/config/zocalo-dev-config.yaml"
        - name: ISPYB_CREDENTIALS
          value: "/zocalo/secrets/ispyb-dev.cfg"
        volumeMounts:
        - name: config-file
          mountPath: /zocalo/config
        - name: secrets
          mountPath: /zocalo/secrets
        - name: dls
          mountPath: /dls
          mountPropagation: HostToContainer
          readOnly: true
      volumes:
      - name: config-file
        configMap:
          name: zocalo-configmap-dev
      - name: secrets
        projected:
          defaultMode: 0444
          sources:
          - secret: 
              name: dev-ispyb-secret
          - secret:
              name: staging-rmq-creds
          - secret:
              name: staging-rmq-api-reader
      - name: dls
        hostPath:
          path: /dls
          type: Directory
